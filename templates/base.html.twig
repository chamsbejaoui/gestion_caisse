<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Système de Gestion de Caisse">
        <title>{% block title %}Welcome!{% endblock %} | Progress Engineering</title>
        <link rel="icon" href="/images/image.png">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
            <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
            <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
            <link href="/css/custom.css" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
            <script src="{{ asset('js/theme.js') }}"></script>
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4 sticky-top">
            <div class="container">
                <a class="navbar-brand d-flex align-items-center flex-shrink-0" href="{{ path('app_home') }}">
                    <img src="/images/progress-logo.svg" alt="Progress Engineering" class="me-2" style="height: 40px;">
                    <span class="brand-text">Système de Gestion Progress</span>
                </a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse w-100" id="navbarNav">
                    <ul class="navbar-nav ms-auto"> <!-- correction ici -->
                        <li class="nav-item mx-1">
                            <a class="nav-link rounded px-3 py-2" href="{{ path('app_home') }}">
                                <i class="fas fa-home me-2"></i> Accueil
                            </a>
                        </li>
                        <li class="nav-item mx-1">
                            <a class="nav-link rounded px-3 py-2" href="{{ path('app_statistiques') }}">
                                <i class="fas fa-chart-bar me-2"></i> Statistiques
                            </a>
                        </li>

                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item mx-1">
                                <a class="nav-link rounded px-3 py-2" href="{{ path('app_admin_index') }}">
                                    <i class="fas fa-users me-2"></i> Gestion des utilisateurs
                                </a>
                            </li>
                            <li class="nav-item mx-1">
                                <a class="nav-link rounded px-3 py-2" href="{{ path('app_categorie_index') }}">
                                    <i class="fas fa-tags me-2"></i> Catégories
                                </a>
                            </li>
                            <li class="nav-item mx-1">
                                <a class="nav-link rounded px-3 py-2" href="{{ path('app_depense_index') }}">
                                    <i class="fas fa-money-bill-wave me-2"></i> Dépenses
                                </a>
                            </li>
                            <li class="nav-item mx-1">
                                <a class="nav-link rounded px-3 py-2" href="{{ path('app_alimentation_index') }}">
                                    <i class="fas fa-coins me-2"></i> Alimentations
                                </a>
                            </li>
                            <li class="nav-item mx-1">
                                <a class="nav-link rounded px-3 py-2" href="{{ path('app_historique') }}">
                                    <i class="fas fa-history me-2"></i> Historique
                                </a>
                            </li>
                            <li class="nav-item mx-1">
                                <a class="nav-link rounded px-3 py-2" href="{{ path('app_caisse_configure') }}">
                                    <i class="fas fa-cogs me-2"></i> Configuration Caisse
                                </a>
                            </li>
                        {% endif %}
                    </ul>

                    <div class="navbar-nav ms-3">
                        <div class="theme-toggle" id="theme-toggle" title="Changer de thème">
                            <i class="fas fa-sun" id="theme-icon"></i>
                        </div>
                    </div>

                    {% if app.user %}
                        <div class="navbar-nav ms-3">
                            <div class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                                    <div class="bg-white rounded-circle p-1 me-2 d-flex align-items-center justify-content-center" style="width: 30px; height: 30px;">
                                        <i class="fas fa-user text-primary"></i>
                                    </div>
                                    <span>{{ app.user.email }}</span>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end shadow border-0">
                                    <li>
                                        <a class="dropdown-item py-2" href="{{ path('app_profile') }}">
                                            <i class="fas fa-user-circle me-2"></i>Mon profil
                                        </a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item py-2 text-danger" href="{{ path('app_logout') }}">
                                            <i class="fas fa-sign-out-alt me-2"></i>Déconnexion
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    {% else %}
                        <div class="navbar-nav ms-3">
                            <a class="nav-link btn btn-outline-light px-3" href="{{ path('app_login') }}">
                                <i class="fas fa-sign-in-alt me-2"></i> Connexion
                            </a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </nav>

        <div class="container">
            <div class="flash-messages my-3">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible fade show shadow-sm border-0" role="alert">
                            <div class="d-flex align-items-center">
                                {% if label == 'success' %}
                                    <div class="alert-icon bg-success bg-opacity-10 rounded-circle p-2 me-3">
                                        <i class="fas fa-check-circle text-success"></i>
                                    </div>
                                {% elseif label == 'error' or label == 'danger' %}
                                    <div class="alert-icon bg-danger bg-opacity-10 rounded-circle p-2 me-3">
                                        <i class="fas fa-exclamation-circle text-danger"></i>
                                    </div>
                                {% elseif label == 'warning' %}
                                    <div class="alert-icon bg-warning bg-opacity-10 rounded-circle p-2 me-3">
                                        <i class="fas fa-exclamation-triangle text-warning"></i>
                                    </div>
                                {% elseif label == 'info' %}
                                    <div class="alert-icon bg-info bg-opacity-10 rounded-circle p-2 me-3">
                                        <i class="fas fa-info-circle text-info"></i>
                                    </div>
                                {% endif %}
                                <div>{{ message }}</div>
                            </div>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>

            {% block body %}{% endblock %}
        </div>

        <footer class="bg-primary text-white py-4 mt-5">
            <div class="container">
                <div class="row">
                    <div class="col-md-6">
                        <div class="d-flex align-items-center mb-3">
                            <img src="/images/progress-logo.svg" alt="Progress Engineering" class="me-2" style="height: 40px; filter: brightness(0) invert(1);">
                            <h5 class="mb-0">Progress Engineering</h5>
                        </div>
                        <p class="mb-0">Une application de gestion de caisse simple et efficace.</p>
                    </div>
                    <div class="col-md-6 text-md-end">
                        <p class="mb-0">&copy; {{ 'now'|date('Y') }} Progress Engineering - Tous droits réservés</p>
                    </div>
                </div>
            </div>
        </footer>
    </body>
</html>
