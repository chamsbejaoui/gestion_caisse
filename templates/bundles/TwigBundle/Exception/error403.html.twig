{% extends 'base.html.twig' %}

{% block title %}Accès refusé{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-danger shadow">
                <div class="card-header bg-danger text-white">
                    <h1 class="h4 mb-0"><i class="fas fa-exclamation-triangle me-2"></i> Accès refusé</h1>
                </div>
                <div class="card-body text-center py-5">
                    <i class="fas fa-lock fa-5x text-danger mb-4"></i>
                    
                    <h2 class="h3 mb-3">Vous n'avez pas les droits nécessaires</h2>
                    <p class="lead mb-4">Vous tentez d'accéder à une page réservée aux administrateurs.</p>
                    
                    <div class="d-grid gap-2 col-md-6 mx-auto">
                        <a href="{{ path('app_home') }}" class="btn btn-primary">
                            <i class="fas fa-home me-2"></i> Retour à l'accueil
                        </a>
                        <a href="{{ path('app_logout') }}" class="btn btn-outline-secondary">
                            <i class="fas fa-sign-out-alt me-2"></i> Déconnexion
                        </a>
                    </div>
                </div>
                <div class="card-footer text-center text-muted">
                    <small>Si vous pensez qu'il s'agit d'une erreur, veuillez contacter l'administrateur système.</small>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}