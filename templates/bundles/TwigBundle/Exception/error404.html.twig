{% extends 'base.html.twig' %}

{% block title %}Page non trouvée{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-warning shadow">
                <div class="card-header bg-warning text-dark">
                    <h1 class="h4 mb-0"><i class="fas fa-exclamation-circle me-2"></i> Page non trouvée</h1>
                </div>
                <div class="card-body text-center py-5">
                    <i class="fas fa-search fa-5x text-warning mb-4"></i>
                    
                    <h2 class="h3 mb-3">Oups! La page demandée n'existe pas</h2>
                    <p class="lead mb-4">L'URL que vous avez saisie n'a pas été trouvée sur ce serveur.</p>
                    
                    <div class="d-grid gap-2 col-md-6 mx-auto">
                        <a href="{{ path('app_home') }}" class="btn btn-primary">
                            <i class="fas fa-home me-2"></i> Retour à l'accueil
                        </a>
                    </div>
                </div>
                <div class="card-footer text-center text-muted">
                    <small>Si vous pensez qu'il s'agit d'une erreur, veuillez contacter l'administrateur système.</small>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}