{% extends 'base.html.twig' %}

{% block title %}Configurer le seuil d'alerte de la caisse{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Configurer le seuil d'alerte</h1>

        <div class="alert alert-info">
            <p class="mb-0">Solde actuel de la caisse : <strong>{{ caisse.solde|number_format(2, ',', ' ') }} €</strong></p>
        </div>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
